#ifndef ARRAY_OPS_H
#define ARRAY_OPS_H

// Header for the max
int find_max(int arr[], int size);

// Header for the min
int find_min(int arr[], int size);

// Header for calculate the average 
double calculate_average(int arr[], int size);

// header for  sort an array using bubble sort
void bubble_sort(int arr[], int size);


#endif // ARRAY_OPS_H

